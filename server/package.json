{
  "name": "chatapp-server",
  "version": "1.0.0",
  "description": "The server for the chat app, mostly just the socket.io code",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "after": "^0.8.2",
    "realm": "^2.2.6",
    "socket.io": "^2.0.4"
  }
}
